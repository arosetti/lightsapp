<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="example_checkbox"
        android:title="@string/pref_title_chkbox"
        android:summary="@string/pref_description_chkbox"
        android:defaultValue="true" />

    <EditTextPreference
        android:key="default_text"
        android:title="@string/pref_title_default_text"
        android:defaultValue="@string/pref_default_default_text"
        android:selectAllOnFocus="true"
        android:inputType="textCapWords"
        android:capitalize="none"
        android:singleLine="true"
        android:maxLines="1" />

    <ListPreference
        android:key="frequency"
        android:title="@string/pref_title_frequency"
        android:entries="@array/pref_frequency_titles"
        android:entryValues="@array/pref_frequency_values"
        android:defaultValue="300"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />
</PreferenceScreen>
